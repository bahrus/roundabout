# Split x to y

It would be useful to add this ability to the list of compacts:

| Pattern | Purpose | RHS Value | Example |
|---------|---------|-----------|---------|
| `split_X_to_Y` | Boolean negation | Delimiter (use space as default) | `negate_isOpen_to_isClosed: 0` |

